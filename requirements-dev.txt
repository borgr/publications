-r requirements.txt
pytest>=7.4
# Bounded to one minor series, unlike the runtime pins. ruff adds rules in every
# release, so an open range would turn an unrelated commit red over a lint rule
# nobody in this repo chose.
ruff>=0.16,<0.17
